<li class="col commentcontent">
  <%= comment.content %> -

  <%= form_for [@case, Comment.new] do |f| %>
      <%= f.text_area :content, placeholder: "Add a Reply" %><br/>
      <%= f.submit "Reply"  %>
      <% end %>

  <ul class="commentholder">
      <%= render partial: 'comments/comment', collection: comment.comments %>
  </ul>

</li>