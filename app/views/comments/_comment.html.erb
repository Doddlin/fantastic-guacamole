<li class="col commentcontent">
  <span class="thecomment"><%= comment.content %> - </span><span class="commentmeta"><%= time_ago_in_words(comment.created_at) %> ago by <%= User.find(comment.user_id).name %></span>
  <ul class="commentholder">
      <%= render partial: 'comments/comment', collection: comment.comments %>
  </ul>

</li>